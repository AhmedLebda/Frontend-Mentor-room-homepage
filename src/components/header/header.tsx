import AppBar from "./app-bar/app-bar"
import Hero from "../hero/Hero"

const Header = () => {
    return (
        <header className="">
            <AppBar />
            <Hero />
        </header>
    )
}

export default Header
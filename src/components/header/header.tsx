import AppBar from "./app-bar/app-bar"

const Header = () => {
    return (
        <header className="bg-zinc-800 min-h-screen">
            <AppBar />
        </header>
    )
}

export default Header